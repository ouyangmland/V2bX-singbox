name: Build and Release

permissions:
  contents: write
  actions: write
  attestations: write
  checks: write
  deployments: write
  discussions: write
  issues: write
  packages: write
  pages: write
  pull-requests: write
  repository-projects: write
  security-events: write
  statuses: write

on:
  workflow_dispatch:
  repository_dispatch:
    types: [new-mod-release]
  push:
    branches:
      - main
      - dev_new
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25.0'
      - name: Get dependencies
        run: go mod download
      - name: Get version
        id: version
        run: |
          EVENT="${{ github.event_name }}"
          if [ "$EVENT" = "release" ]; then
            VERSION="${{ github.ref_name }}"
          elif [ "$EVENT" = "repository_dispatch" ]; then
            TAG="${{ github.event.client_payload.tag }}"
            VERSION="${TAG#v}"
            VERSION="${VERSION#*-}"
          else
            VERSION="${GITHUB_SHA:0:8}"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "VERSION=$VERSION" >> $GITHUB_ENV
          echo "EVENT=$EVENT" >> $GITHUB_OUTPUT
      - name: Build amd64
        run: |
          CGO_ENABLED=0 GOARCH=amd64 GOEXPERIMENT=jsonv2 go build -ldflags="-X github.com/ouyangmland/V2bX-singbox/cmd.version=${VERSION} -s -w" -o "V2bX-linux-amd64" .
          ls -lh V2bX-linux-amd64
      - name: Build arm64
        run: |
          CGO_ENABLED=0 GOARCH=arm64 GOEXPERIMENT=jsonv2 go build -ldflags="-X github.com/ouyangmland/V2bX-singbox/cmd.version=${VERSION} -s -w" -o "V2bX-linux-arm64" .
          ls -lh V2bX-linux-arm64
      - name: Prepare amd64
        run: |
          mkdir -p build_amd64
          cp README.md LICENSE build_amd64/ 2>/dev/null || true
          cp example/*.json build_amd64/ 2>/dev/null || true
          cp V2bX-linux-amd64 build_amd64/V2bX
          cd build_amd64
          zip -9vr "../V2bX-linux-amd64-${VERSION}.zip" . -x "*.DS_Store"
      - name: Prepare arm64
        run: |
          mkdir -p build_arm64
          cp README.md LICENSE build_arm64/ 2>/dev/null || true
          cp example/*.json build_arm64/ 2>/dev/null || true
          cp V2bX-linux-arm64 build_arm64/V2bX
          cd build_arm64
          zip -9vr "../V2bX-linux-arm64-${VERSION}.zip" . -x "*.DS_Store"
      - name: Upload amd64
        uses: actions/upload-artifact@v4
        with:
          name: V2bX-linux-amd64
          path: ./V2bX-linux-amd64-${VERSION}.zip
      - name: Upload arm64
        uses: actions/upload-artifact@v4
        with:
          name: V2bX-linux-arm64
          path: ./V2bX-linux-arm64-${VERSION}.zip
      - name: Get tag for release
        id: get_tag
        run: |
          if [ "${{ github.event_name }}" = "release" ] || [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            TAG="${{ github.ref_name }}"
          elif [ "${{ github.event_name }}" = "repository_dispatch" ]; then
            TAG="${{ github.event.client_payload.tag }}"
          else
            TAG=""
          fi
          echo "tag=$TAG" >> $GITHUB_OUTPUT
          echo "tag=$TAG" >> $GITHUB_ENV

      - name: Create Release
        if: steps.get_tag.outputs.tag != ''
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.get_tag.outputs.tag }}
          name: V2bX ${{ steps.get_tag.outputs.tag }}
          draft: false
          prerelease: false
          files: |
            ./V2bX-linux-amd64-${VERSION}.zip
            ./V2bX-linux-arm64-${VERSION}.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
